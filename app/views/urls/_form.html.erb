<%= form_for(@url, remote: true) do |f| %>
  <% if @url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
      <% @url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url, :placeholder => "Must be a valid http or https URL string." %>
  </div>
<!--   <div class="field">
    <%= f.label :shortened_url %><br>
    <%= f.text_field :shortened_url %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
